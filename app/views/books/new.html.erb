<% content_for :head do %> 
<%= render :partial => "index_menu" %>
<% end %>

<%= error_messages_for :book %>

<%= form_for @book do |f| %>

<%= render_form('form', f) %>

<p> <%= f.submit 'Create' %> </p>
    
<% end %>
