<h1>Book#Loans#new</h1>
<%= render :partial => "books/show_menu" %>

<%= error_messages_for :loan %>

<%= form_for [@book, @loan] do |f| %>

<%= book_loans_form(f) %>

<p> <%= f.submit 'Create' %> </p>

<% end %>
