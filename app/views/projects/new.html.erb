<% content_for :head do %> 
  <%= render :partial => "index_menu" %>
<% end %>

<%= error_messages_for :project %>

<%= form_for @project do |f| %>

  <%= render_form('form', f) %>

  <div>
    <%= f.submit "Create" %>
  </div>

<% end %>
