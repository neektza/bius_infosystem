<% content_for :head do %> 
<%= render :partial => "show_menu" %>
<% end %>

<%= error_messages_for :section %>

<%= form_for @section do |f| %>

<%= render_form('form', f) %>

<p><%= f.submit "Update" %></p>

<% end %>

