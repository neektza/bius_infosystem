<div> 
  Tip: <%= f.select :in_out, {"Ulazni" => Transfer::TYPE[:incoming], "Izlazni" => Transfer::TYPE[:incoming]}%> 
</div>

<div>
  Iznos:<br/>
  <%= f.text_field :amount %>
</div>

<div>
  Datum:<br/>
  <%= f.date_select :date , :order => [:day, :month, :year], :start_year => 2000  %>
</div>

<div>
  Svrha:<br/>
  <%= f.text_area :purpose, :rows => 10 , :cols=> 20 %>
</div>

<div>Crno? | <%= f.check_box :is_secret %></div>

<div><%= f.hidden_field :member_id, :value => session[:user_id] %></div>

