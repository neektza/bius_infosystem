<h1>Transfers#new</h1>
<%= render :partial => "index_menu" %>

<%= error_messages_for :transfer %>

<% form_for @transfer do |f| %>

<%= transfer_form(f) %>

<p><%= f.submit 'Create' %></p>

<% end %>
