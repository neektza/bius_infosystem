<% content_for :head do %> 
<%= render :partial => "welcome_menu" %>
<% end %>

<%= error_messages_for :user %>

<% form_for :user, :url => user_url, :html => {:method => :post} do |f| %>

<%= user_form(f) %>

<p><%= submit_tag 'Register' %></p>

<% end %>
