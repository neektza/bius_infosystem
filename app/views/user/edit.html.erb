<h1>User#edit</h1>
<%= render :partial => "profile_menu" %>

<%= error_messages_for :user %>

<% form_for :user, :url => user_url, :html => {:method => :put} do |f| %>

<%= user_form(f) %>

<p><%= submit_tag 'Update' %></p>
    
<% end %>
