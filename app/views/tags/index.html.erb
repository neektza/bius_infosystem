<h1>Tags#index</h1>
<%= render :partial => "index_menu" %>
<hr/>

<% unless @tags.empty? %>

<p>
<ul>
	<% for tag in @tags %> 
    <li><%= link_to tag.title, tag_url(tag) %></li>
	<% end %>
</ul>
</p>

<% end %>
